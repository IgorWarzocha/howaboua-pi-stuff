---
"@howaboua/pi-rescue": minor
---

Add `/rescue`, a bounded tool-free compaction command with configurable model, provider, and reasoning. Preserve rescue summarization usage in Pi compaction entries so session totals include it.
