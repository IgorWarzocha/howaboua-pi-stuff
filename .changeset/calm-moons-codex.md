---
"@howaboua/pi-codex-conversion": patch
---

Syncs the Codex provider with Pi 0.80.6 and uses GPT-5.6 Luna by default for web search, image descriptions, and native compaction. Adds the GPT-5.6 helper model choices, max reasoning, zstd SSE requests, current Responses usage accounting, stale WebSocket rotation, an opt-in Responses Lite beta for GPT-5.6 turns and native compaction, and the current Codex apply-patch engine behind Pi's local-only filesystem and structured-output adapters.
