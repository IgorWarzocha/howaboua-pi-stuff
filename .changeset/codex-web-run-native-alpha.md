---
"@howaboua/pi-codex-conversion": patch
---

Routes `web_run` through the Rust path tool only, removes the TypeScript fetch fallback and browser-style Cloudflare cookie handling, and preserves explicit alpha search URLs.
