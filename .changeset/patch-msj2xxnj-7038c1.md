---
"@howaboua/pi-codex-conversion": patch
---

Add cross-platform persistent Deno Notebook Code Mode with deliberate `globalThis` project state, private delta session checkpoints that respect deleted project bindings, named reusable profiles, reanimated helpers, budget-rotated recoverable Jupyter journals, approval-guided exact-version npm imports, visible memory telemetry, inspectable and transactionally pinnable retained state, explicit glob pruning, auditable expandable cell traces, one-shot Deno diagnostics, bounded Jupyter transport operations, isolated concurrent project sessions, accurate failed-cell cleanup, cancellable startup and restoration, live heap/profile configuration refresh, ownership-safe lock leases, validated persisted bindings, and host-side lifecycle controls. Add gated Daybreak Blue and Daybreak Red cybersecurity models with Code Mode transport support and require Pi 0.84.2 or newer. Preserve namespaced Codex tool calls, group Codex Responses Lite tools under the standard `functions` namespace, retain qualified nested tool identities through both Code and Notebook runtimes, preserve terminal end-turn diagnostics, and honor Pi's opt-in strict JSON-schema constrained sampling for normal and Notebook controls without changing ordinary Codex tool requests. Activate ChatGPT Codex priority processing in Fast Mode with the required first-party originator and routing hint across WebSocket and HTTP transports while preserving normal request identity and clearing inherited priority hints otherwise. Update Undici to the patched 8.10 release.
