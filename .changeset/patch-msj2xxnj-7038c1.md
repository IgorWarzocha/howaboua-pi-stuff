---
"@howaboua/pi-codex-conversion": patch
---

Add cross-platform persistent Deno Notebook Code Mode with deliberate `globalThis` project state, private delta session checkpoints, named reusable profiles, reanimated helpers, append-oriented recoverable Jupyter journals, approval-guided exact-version npm imports, visible memory telemetry, inspectable and pinnable retained state, explicit glob pruning, auditable expandable cell traces, one-shot Deno diagnostics, bounded Jupyter transport operations, isolated concurrent project sessions, accurate failed-cell cleanup, cancellable kernel restarts, ownership-safe lock leases, validated persisted bindings, atomic pinning, and host-side lifecycle controls. Activate ChatGPT Codex priority processing in Fast Mode with the required first-party originator and routing hint across WebSocket and HTTP transports while preserving normal request identity and clearing inherited priority hints otherwise. Update Undici to the patched 8.10 release.
