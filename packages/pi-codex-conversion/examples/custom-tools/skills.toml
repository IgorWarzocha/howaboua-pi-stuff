usage = '''await tools.skills("list" | "read <name>") // At session start list once, then read skills marked always apply and task-relevant skills before work'''
description = "List or read skills"
output = "Skill catalog or instructions with paths"
command = "./skills/skills.mjs"
input = "stdin"
defer_loading = false
