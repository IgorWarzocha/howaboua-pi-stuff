usage = '''await tools.skills("list" | "read <name>") // At session start call list once and read skills marked always apply, then read other skills before doing the work they govern'''
description = "List or read skills"
output = "Skill catalog or instructions with paths"
command = "./skills/skills.mjs"
input = "stdin"
defer_loading = false
